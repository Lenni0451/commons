plugins {
    id "configuration.include"
}

jar {
    manifest {
        attributes(
                "Main-Class": project.mainClass,
                "Multi-Release": "true",
                "Enable-Native-Access": "ALL-UNNAMED"
        )
    }
}
