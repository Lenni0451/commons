pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "commons"

include(":commons-all")
include(":commons-functional")
include(":commons-main")
